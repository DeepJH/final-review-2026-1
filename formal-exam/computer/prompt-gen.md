# 设定
你是一名资深计算机组成原理老师，请为我补全复习资料。
# 输入
- 任务
    模仿完成部分的风格，修正格式补全复习资料中计算题的内容。
- 复习资料
```markdown
# [ ]计算题
- [ ]画出连接图，地址线，数据线，片选数据线，片选逻辑
    1. SRAM 若干, 4k \* 8bit 。用这个做一个 8k \* 16bit 。画出：连接图，地址线，数据线，片选数据线，片选逻辑。
        - answer

    2. 16K \* 8bit 做 16k \* 8bit 画出连接图，地址线，数据线，片选数据线，片选逻辑
        - answer
- [ ]各种码的简单变量计算
    1. x = -0.1011 y = 0.1101 用变形补码双符号位计算 x + y , 并指出结果是否溢出。
    2. x = -0.1110 y = 0.1101 用原码移位乘法计算 x * y 的原码，要过程。
- [ ]求平均存取时间和访问效率
    3. 一个用cache的存储系统，cache 存取周期 10 ns , 主存存取周期 100 ns ,在一次程序存储过程中，CPU访问cache的命中率95%，求 cache 或 主存 平均存储时间和访问效率。
    8. 一存储系统，主存存取时间为200ns，cache 存取时间 20ns，cache命中率 98%，求平均存取时间和访问效率
4.   
    4. 一个系统，主存 16 MB ， cache 8 KB 直接映射方式，每块大小 16 B ，计算主存地址的字段划分。字段划分包括 标记 块索引 块内地址各占多少位。
    9. 主存 4 MB ，cache 16KB ， 每块 32 B ， 用4路主相连映射，计算主存地址的字段划分。标记 组索引 块内地址各占多少位。
5.     
    5. 一总线在一个总线周期，可并行传 8 B 数据，假设 1 总线周期=4时钟周期，总线时钟频率 100 MHz，求最大数据传输率(MB/S)
    10. 总线频率 66 MHz 1时钟周期内传输1次数据，每次 4 B，求总线最大数据传输率？如果在32位总线基础上，变64位，其他不变，新总线带宽多少？
6. 
    6. 求有效信息为 01101110 的海明校验码，要过程。
    7. ieee 1754标准的32位单精度浮点数，表示为 0xc1E80000 H（Hex）求10进制是多少

# [x]填空题
- [x]一个数变原码反码补码 ，8位
    - answer  
        - | 编码类型 | 最小值（二进制） | 最小值（十进制） | 最大值（二进制） | 最大值（十进制） | 数值范围 |
            | --- | --- | --- | --- | --- | --- |
            | **原码** | `11111111` | **-127** | `01111111` | **+127** | $-127 \sim +127$ |
            | **反码** | `10000000` | **-127** | `01111111` | **+127** | $-127 \sim +127$ |
            | **补码** | `10000000` | **-128** | `01111111` | **+127** | $-128 \sim +127$ |
        - note
            - 计算
                - 这些码都是 8 位长度。第 0 位是`负数标记`。后面才真正计数。
                - 原码就最基本的二进制。
                - 反码正数同原码，负数取反。
                - 补码正数同原码，负数为反码，直接加 1 。
            - 哲学
                - 反码是为了方便计算，因为反了之后，计算可以直接异或了，不用搞一大堆规则了。
                - 补码是为了利用反码中的 -0 ，把 -0 当成 -1 用。
                - 原码 -1 10000001
                    反码 -1 11111110
                    补码 -1 11111111
- [x]计算机存储器层次结构，主要基于程序访问的什么原理。主要基于{answer}，从上到下存储器的容量逐渐{answer}速度逐渐{answer}。
    - answer
        - 局部性原理
        - 增大
        - 变慢
- [x]一指令的执行过程，通常包括{answer}阶段。通常包括 
    - answer
        - 取址
        - 译码
        - 执行
        - 中断
        - 等几个周期
```
# 输出
- 补全内容后的 md 复习资料文件。
1. 
    1. SRAM 若干, 4k \* 8bit 设计成8k \* 16bit 画出连接图，地址线，数据线，片选数据线，片选逻辑
    2. 16K \* 8bit 做 16k \* 8bit 画出连接图，地址线，数据线，片选数据线，片选逻辑 [TODO]
2. 
    1. x = -0.1011 y = 0.1101 用变形补码双符号位计算 x + y , 并指出结果是否溢出。
    2. x = -0.1110 y = 0.1101 用原码移位乘法计算 x * y 的原码，要过程。
3.
    3. 一个用cache的存储系统，cache 存取周期 10 ns , 主存存取周期 100 ns ,在一次程序存储过程中，CPU访问cache的命中率95%，求 cache 或 主存 平均存储时间和访问效率。
    8. 一存储系统，主存存取时间为200ns，cache 存取时间 20ns，cache命中率 98%，求平均存取时间和访问效率
4.   
    4. 一个系统，主存 16 MB ， cache 8 KB 直接映射方式，每块大小 16 B ，计算主存地址的字段划分。字段划分包括 标记 块索引 块内地址各占多少位。
    9. 主存 4 MB ，cache 16KB ， 每块 32 B ， 用4路主相连映射，计算主存地址的字段划分。标记 组索引 块内地址各占多少位。
5.     
    5. 一总线在一个总线周期，可并行传 8 B 数据，假设 1 总线周期=4时钟周期，总线时钟频率 100 MHz，求最大数据传输率(MB/S)
    10. 总线频率 66 MHz 1时钟周期内传输1次数据，每次 4 B，求总线最大数据传输率？如果在32位总线基础上，变64位，其他不变，新总线带宽多少？
6. 
    6. 求有效信息为 01101110 的海明校验码，要过程。
    7. ieee 1754标准的32位单精度浮点数，表示为 0xc1E80000 H（Hex）求10进制是多少
7. 
    11. 重要！简述程序中断方式和DMA方式在数据传送过程中的主要区别。
    12. 重重要！解释指令\机器\时钟周期，概念和关系
    13. 简述硬布线控制器和微程序控制器的优缺点。
    14. 简述什么是寻址方式，列举至少4种常见数据寻址方式。简要说明特点。(只写最简单的4种)
8. 
    15. 一个数变原码反码补码 ，8位
    16. 计算机存储器层次结构，主要基于程序访问的什么原理。主要基于局部性原理，从上到下存储器的容量逐渐增大，变慢。
    17. 一指令的执行过程，通常包括？阶段。通常包括 取址 译码 执行 中断 等几个周期
    18. 一种DMA传送方式中，DMA控制器完全接管总线，连续传送一整块数据后，才释放总线控制权，这叫什么传送方式。叫成组传送/块传送。
    19. cache和主存地址映射方式有：直接映射，全相连映射，主相连映射。
    20. 计算机执行程序时，指令的地址通常是由？提供。是由PC。
9. [TODO]
    21. 冯·诺伊曼的核心思想是？存储程序。运算器的核心是加法器。
    22. 计算机参与运算的是补码，最适合加减运算。
    23. CPU能直接访问的存储器：内存
    24. 指令的地址码直接给出操作数在内存中的地址，叫直接寻址
    25. 在CPU中，指令译码器是控制器的核心部件之一
    26. 允许多个部件同时在？上传送数据的总线结构是：单总线结构
    27. 程序查询方式，程序中断方式，DMA方式，通道方式，这4中io控制方式中，CPU与外部设备并行工作程度最高的是：通道方式。
    28. 衡量计算机运算速度的常用单位是MIPS每秒指令每秒
    29. 8位机器数的原码反码补码范围。最大最小。
    30. CPU和主存之间，解决速度不匹配的问题的是：cache高速缓存
    31. 可能要做错。某存储器芯片有12地址线和4数据线，存储容量是多少？4K*4[TODO]
    32. Aa的ascii码是多少，注意可能要Hex。
    33. 硬布线和微程序控制器相比，优点是什么：更快。
    34. 课上没讲过。在指令流水线中，由于一条指令的写后读操作，导致流水线停顿，这属于：数据相关
    35. 精简指令集RISC，错的是：
        - 指令长度固定：对
        - 寻址方式少：对
        - 只有取数存数指令才访问内存：对
        - 指令系统复杂，功能强大：错
    36. USB总线的中文全称是：通用串行总线
    37. 判断题没有，的老师有点傻逼，喜欢偷偷改和抠字眼，看仔细了。
    38. 0的各种码表示形式，哪个是唯一的。